const initState = {
    showSettings: false,
    sidebarLogo: '',
    fixedHeader: false,
    tagsView: []
} 
export default function settings(state = initState, action) {
    switch(action.type) {
        default:
            return state
    }
}